{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://requirements-yaml.example/requirements.schema.json",
  "title": "Compact requirements document",
  "description": "Optional editor aid for a requirements.yaml in the requirements-yaml format. Structural only — it enforces no grammar and gates nothing. Authoring and review rules live in the requirements-yaml skill (SKILL.md).",
  "markdownDescription": "Compact requirements. One line each: `<ID>: <capability> | so that <value> | verified when <observable proof>.` Grouped into `functional` (area → `As a <role>, I can:` → ID), `non_functional` (quality → ID), optional `deferred`, and project domain sections. Full rules: the **requirements-yaml** skill.",
  "type": "object",
  "minProperties": 1,
  "properties": {
    "functional": {
      "type": "object",
      "description": "Functional requirements grouped by system area, then (recommended) by story role.",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/functionalArea" },
      "propertyNames": { "$ref": "#/$defs/snakeCaseKey" }
    },
    "non_functional": {
      "type": "object",
      "description": "Non-functional requirements grouped by quality attribute.",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/requirementMap" },
      "propertyNames": { "$ref": "#/$defs/snakeCaseKey" }
    },
    "deferred": {
      "type": "object",
      "description": "Explicitly deferred / future requirements grouped by area.",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/deferredRequirementMap" },
      "propertyNames": { "$ref": "#/$defs/snakeCaseKey" }
    }
  },
  "additionalProperties": { "$ref": "#/$defs/domainSection" },
  "propertyNames": { "$ref": "#/$defs/snakeCaseKey" },
  "$defs": {
    "snakeCaseKey": {
      "description": "A lower snake_case grouping key such as `accounts`, `data_policy`, or `openness_and_governance`.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$"
    },
    "roleKey": {
      "description": "A role heading: `As a <role>, I can`, so requirement lines can start with the capability verb.",
      "type": "string",
      "pattern": "^As .+, I can$"
    },
    "requirementId": {
      "description": "A stable requirement ID: uppercase area prefix plus a zero-padded number.",
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9]*-[0-9]{2,}$"
    },
    "deferredRequirementId": {
      "description": "A stable future-scope ID: the normal ID prefixed with `[deferred] `.",
      "type": "string",
      "pattern": "^\\[deferred\\] [A-Z][A-Z0-9]*-[0-9]{2,}$"
    },
    "requirementText": {
      "description": "One requirement line. Recommended: `<capability> | so that <value> | verified when <observable proof>.` Under a role heading, start with the verb. Quote only if a clause contains `: ` or the value starts with a YAML indicator.",
      "type": "string",
      "minLength": 1
    },
    "requirementMap": {
      "description": "A map of requirement IDs to one-line requirement text.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/requirementText" },
      "propertyNames": { "$ref": "#/$defs/requirementId" }
    },
    "deferredRequirementMap": {
      "description": "A map of deferred requirement IDs to one-line requirement text. Used only under `deferred`.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/requirementText" },
      "propertyNames": { "$ref": "#/$defs/deferredRequirementId" }
    },
    "functionalArea": {
      "description": "A system-area section: group by role heading (`As a <role>, I can`) whose value is a requirement map, or put a requirement map directly if roles are not used.",
      "type": "object",
      "minProperties": 1,
      "anyOf": [
        {
          "additionalProperties": { "$ref": "#/$defs/requirementMap" },
          "propertyNames": { "$ref": "#/$defs/roleKey" }
        },
        { "$ref": "#/$defs/requirementMap" }
      ]
    },
    "domainSection": {
      "description": "A project-specific top-level section: snake_case subgroups whose values are requirement maps, like `non_functional`.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/requirementMap" },
      "propertyNames": { "$ref": "#/$defs/snakeCaseKey" }
    }
  }
}
